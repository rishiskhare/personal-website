export interface ExperienceItem {
  company: string;
  title: string;
  period: string;
  details: string[];
}

export interface ProjectItem {
  name: string;
  period: string;
  details: string[];
}

export interface InventionItem {
  name: string;
  description: string;
  status: string;
}
